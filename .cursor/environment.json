{
  "name": "Product Ops Workspace",
  "install": "npm install && npx prisma migrate deploy && npx prisma generate && npx tsx prisma/seed.ts --if-empty",
  "terminals": [
    {
      "name": "app",
      "command": "npm run dev"
    }
  ]
}
